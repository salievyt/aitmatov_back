# Переопределения для локальной разработки
# Используйте: docker-compose up web db

version: '3.9'

services:
  web:
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=True
